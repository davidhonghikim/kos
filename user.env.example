# =============================================================================
# kOS User-Specific Environment Overrides
# =============================================================================
# Copy this file to user.env and add your personal configurations
# This file overrides .env values and adds user-specific keys
# DO NOT commit user.env to git (it's in .gitignore)

# =============================================================================
# USER-SPECIFIC API KEYS (Override .env values)
# =============================================================================
# Replace with your actual API keys
KOS_OPENAI_API_KEY=sk-your-actual-openai-key-here
KOS_ANTHROPIC_API_KEY=sk-ant-your-actual-anthropic-key-here
KOS_GOOGLE_API_KEY=your-actual-google-api-key-here

# =============================================================================
# USER-SPECIFIC SECURITY (Override .env values)
# =============================================================================
# Generate a secure JWT secret for your environment
KOS_JWT_SECRET=your-super-secure-jwt-secret-generated-for-your-environment

# =============================================================================
# USER-SPECIFIC PASSWORDS (Override .env values)
# =============================================================================
# Database passwords for your local environment
KOS_POSTGRES_PASSWORD=your-secure-postgres-password
KOS_REDIS_PASSWORD=your-secure-redis-password
KOS_NEO4J_PASSWORD=your-secure-neo4j-password
KOS_MINIO_ROOT_PASSWORD=your-secure-minio-password
KOS_GRAFANA_ADMIN_PASSWORD=your-secure-grafana-password

# =============================================================================
# USER-SPECIFIC PORTS (Override if needed)
# =============================================================================
# Uncomment and modify if you need different ports
# KOS_API_PORT=8001
# KOS_FRONTEND_PORT=3001
# KOS_POSTGRES_PORT=5433
# KOS_REDIS_PORT=6380

# =============================================================================
# USER-SPECIFIC DOCKER IMAGES (Override for testing)
# =============================================================================
# Uncomment and modify for testing different versions
# KOS_POSTGRES_IMAGE=postgres:16-alpine
# KOS_REDIS_IMAGE=redis:8-alpine
# KOS_NEO4J_IMAGE=neo4j:5.16-community

# =============================================================================
# USER-SPECIFIC FEATURE FLAGS (Override .env values)
# =============================================================================
# Enable/disable services for your development needs
# KOS_ENABLE_ELASTICSEARCH=true
# KOS_ENABLE_WEAVIATE=true
# KOS_ENABLE_MINIO=true
# KOS_ENABLE_PROMETHEUS=true
# KOS_ENABLE_GRAFANA=true

# =============================================================================
# USER-SPECIFIC EMAIL (Override .env values)
# =============================================================================
# Your email configuration
KOS_SMTP_USER=your-email@gmail.com
KOS_SMTP_PASSWORD=your-app-specific-password
KOS_EMAIL_FROM=your-email@gmail.com

# =============================================================================
# USER-SPECIFIC PATHS (Override .env values)
# =============================================================================
# Custom paths for your environment
# KOS_STORAGE_PATH=./your-custom-storage-path
# KOS_BACKUP_PATH=./your-custom-backup-path
# KOS_PLUGIN_DIRECTORY=./your-custom-plugin-path

# =============================================================================
# USER-SPECIFIC DEVELOPMENT SETTINGS
# =============================================================================
# Development-specific overrides
KOS_DEBUG=true
KOS_LOG_LEVEL=DEBUG
KOS_MOCK_EXTERNAL_SERVICES=true
KOS_ENABLE_DEBUG_LOGGING=true

# =============================================================================
# USER-SPECIFIC ADDITIONS (Not in .env)
# =============================================================================
# Add any additional environment variables specific to your setup
# KOS_CUSTOM_VARIABLE=your-custom-value
# KOS_DEVELOPMENT_MODE=true
# KOS_TEST_API_KEY=your-test-key 